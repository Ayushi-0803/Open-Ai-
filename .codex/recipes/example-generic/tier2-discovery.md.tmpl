# {{domain_name}} Discovery Agent

Use the pattern file for `{{domain_name}}` to classify the codebase into domain-owned symbols, shared symbols, and human-review edge cases.

Write:
- `discovery.{{domain}}.json`
- `discovery.{{domain}}.md`
