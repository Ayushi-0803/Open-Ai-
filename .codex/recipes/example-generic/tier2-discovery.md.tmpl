# {{domain_name}} Discovery Agent

Validate the deterministic ownership for `{{domain_name}}`.

Keep the existing machine-readable contract and only refine:
- ownership rationale
- shared candidates
- cross-domain dependencies
- risk notes

Do not change filenames:
- `discovery.{{domain}}.json`
- `discovery.{{domain}}.md`
