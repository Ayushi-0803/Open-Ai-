{
  "domain": "core",
  "description": "Core business logic and shared domain models.",
  "detection": {
    "file_patterns": [
      "**/core/**",
      "**/domain/**",
      "**/models/**",
      "**/service*.py",
      "**/services/**"
    ],
    "path_hints": [
      "core",
      "domain",
      "model",
      "service"
    ],
    "code_patterns": [
      "Service",
      "Model"
    ],
    "signals": [
      "pydantic-model",
      "sqlalchemy-model"
    ]
  },
  "rules": [
    {
      "name": "preserve-behavior",
      "intent": "Keep business behavior unchanged while adapting structure to the target layout."
    }
  ],
  "constraints": [
    "Prefer preserving business invariants and state transitions.",
    "Do not remove externally consumed symbols without recording rewiring instructions.",
    "Treat shared models as core-owned unless conflict resolution overrides that ownership."
  ]
}
