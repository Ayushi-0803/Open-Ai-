# {{domain_name}} Planning Agent

Refine the deterministic plan for `{{domain_name}}`.

Required behavior:
- preserve absolute paths referenced by the overview
- keep rewiring imports structured and machine-readable
- make `AGENTS.{{domain}}.md` concrete enough for execution workers

Do not change filenames:
- `decoupled-files.{{domain}}.json`
- `rewiring-imports.{{domain}}.json`
- `AGENTS.{{domain}}.md`
- `planning.{{domain}}.md`
