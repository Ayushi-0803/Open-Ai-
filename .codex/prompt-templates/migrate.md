---
description: Alias of migration for Codex custom prompts
argument-hint: [SOURCE=<path>] [TARGET=<path>] [RECIPE=<id-or-path>]
---

Run the same workflow as `/prompts:migration`.

Use the migration framework in the current repo if present, otherwise use the nested `Open-Ai-/` repo if present.
